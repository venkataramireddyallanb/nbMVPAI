# nicheBrains backend — production requirements
# Pinned to versions known to work together as of 2026-04.

# --- Core web framework ---
Flask==3.0.3
Flask-RESTX==1.3.0
Flask-Cors==4.0.1
Flask-JWT-Extended==4.6.0
Flask-Migrate==4.0.7
python-dotenv~=1.1.1
Werkzeug==3.0.3

# --- ORM / DB ---
SQLAlchemy~=2.0.36
Flask-SQLAlchemy==3.1.1
alembic==1.13.2

# --- Validation / serialization ---
marshmallow==3.21.3
marshmallow-sqlalchemy==1.0.0
pydantic~=2.11.9

# --- Auth / security ---
bcrypt>=4.0.1
PyJWT>=2.0,<3.0

# --- AI / agents ---
# CrewAI is intentionally pinned. The framework is used with EXACTLY 2 LLM agents
# (Narrator + Instant-Report Drafter) per Acceptance Criterion #10 of the spec.
# openai 2.x is required by crewai 1.14+; langchain-openai 0.3+ is the first
# release that supports openai 2.x, so both are bumped together.
crewai==1.14.2
langchain-openai>=0.3.0,<0.4.0
openai>=2.0.0,<3.0.0
tiktoken>=0.7.0,<1.0.0

# --- Utilities ---
python-dateutil==2.9.0.post0
ulid-py==1.1.0

# --- Testing ---
pytest==8.2.1
pytest-flask==1.3.0
pytest-cov==5.0.0
factory-boy==3.3.3

# --- Python 3.13 transitive guards -----------------------------------
# These are TRANSITIVE deps of CrewAI / OpenAI; pinning lower bounds
# avoids old C-extension wheels that fail on Python 3.13 with
#   TypeError: Can't replace canonical symbol for '__firstlineno__'
chromadb>=0.5.20
tokenizers>=0.20.0
pydantic-core>=2.27.0
